FROM docker.io/library/debian:bookworm-slim

# Install podman remote client to command host checkpoint/restores
RUN apt-get update && apt-get install -y \
    podman \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Copy the compiled C counter app into system PATH
COPY src/counter /usr/local/bin/counter

# Copy tests layout
COPY tests/run_tests.sh /usr/local/bin/run_tests
COPY tests/ /tests/

RUN chmod +x /usr/local/bin/run_tests /tests/*.sh

ENTRYPOINT ["/usr/local/bin/run_tests"]